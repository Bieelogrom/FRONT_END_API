import styled from 'styled-components'

const MainWrapper = styled.main`
    width: 100vw;
    height: 100vh;
`

const Logado = () => {
    return (
        <MainWrapper>
            
        </MainWrapper>
    )
}

export default Logado;